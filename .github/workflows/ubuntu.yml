name: Ubuntu_Server
on: 
  push:
  workflow_dispatch:
defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: B4gol/deckflows/ssh@decent
      with:
        ssh_key: ${{ secrets.SSH_KEY }}
        key_file: id_ecdsa
    - uses: B4gol/action-tmate@v1
    - run: source ubuntu-run.sh
    - name: Setup Tmate Edition
      timeout-minutes: 340
