name: Ubuntu_Server
on: 
  push:
  workflow_dispatch:
defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: B4gol/deckflows/ssh@decent
      with:
        ssh_key: ${{ secrets.SSH_KEY }}
        key_file: id_ecdsa
    - uses: B4gol/action-tmate@master
    - run: source ubuntu-run.sh
    - name: Setup Tmate Ubuntu
      timeout-minutes: 340
